{
  "name": "latchkey",
  "version": "0.0.1",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "deploy": "bash deploy/deploy.sh",
    "deploy:contract": "bash deploy/deploy-contract.sh"
  }
}
